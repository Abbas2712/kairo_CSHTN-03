import 'package:flutter/material.dart';

class Qtag extends StatelessWidget {
  const Qtag({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}